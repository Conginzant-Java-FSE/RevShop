package com.revature.revshop.dto;

public class BuyerDTO extends UserDTO {
    // currently no buyer specific fields, but architecture allows for them later
    // (eg. loyalty points)

    public BuyerDTO() {
        super();
    }

}
